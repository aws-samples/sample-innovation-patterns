node-linker=hoisted
# Dependency install cooldown — block resolving versions published in the last day/week.
# pnpm reads minimumReleaseAge (minutes); npm v11.10+ reads min-release-age (days).
# Both keys are set so the cooldown holds regardless of which client runs install.
minimumReleaseAge=1440
min-release-age=7
