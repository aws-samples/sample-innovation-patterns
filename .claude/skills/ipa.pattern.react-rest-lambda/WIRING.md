# React REST Lambda Pattern — Inter-Stack Wiring Map
# Updated incrementally as stack skills are added.
#
# Format: source stack output → target stack parameter or env var
# ipa.compose reads this to generate Makefile parameter-overrides.

wiring: []
# No wiring entries yet — ECR's downstream consumers (Lambda) don't exist.
# When Spec 4 (Lambda) is implemented, 2 entries will be added here.
