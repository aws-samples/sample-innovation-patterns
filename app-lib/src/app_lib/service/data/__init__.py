"""Data services for entity persistence.

Contains abstract base classes and concrete implementations for
data access. Each entity type has its own subdirectory with a
data service that inherits from AbstractDataService[T].
"""
