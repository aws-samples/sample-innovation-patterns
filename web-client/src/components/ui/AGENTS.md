# components/ui/

shadcn/ui components. **Never edit directly**.

Install new components:
- MCP: Use shadcn MCP server tools
- CLI: `npx shadcn@latest add <component>`

For customization, create wrapper components in `src/components/`.
