# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence

# ASH security scanning component
include:
  - component: code.aws.dev/proserve/automated-security-helper/automated-security-helper/ash@~latest

# Any publicly available python image
image: public.ecr.aws/docker/library/python:3.12-bookworm

stages:
  - test
#   - securityScan  # Inherited
  - deploy
  - release

# GitLab Pages deployment job - must be named "pages" to trigger Pages deployment
# Builds Docusaurus documentation and publishes to https://<namespace>.gitlab.io/<project-name>/
pages:
  stage: deploy
  needs: []
  image: public.ecr.aws/docker/library/node:22-bookworm
  variables:
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
    KUBERNETES_MEMORY_REQUEST: "4Gi"
    KUBERNETES_MEMORY_LIMIT: "4Gi"
    KUBERNETES_EPHEMERAL_STORAGE_REQUEST: "4Gi"
    KUBERNETES_EPHEMERAL_STORAGE_LIMIT: "4Gi"
    NODE_OPTIONS: "--max-old-space-size=3072"
  before_script:
    - corepack enable
    - corepack prepare pnpm@latest --activate
    - cd docs
    - pnpm install --frozen-lockfile
  script:
    - pnpm exec docusaurus build
    - mv build ../public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

# Auto-tag on merge to main when VERSION was bumped (triggers github-release job)
auto-tag:
  stage: release
  image: public.ecr.aws/docker/library/python:3.12-bookworm
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH && $CI_PIPELINE_SOURCE != "push" && $CI_COMMIT_TAG == null
      when: on_success
    - when: never
  script:
    - VERSION=$(tr -d '[:space:]' < VERSION)
    - |
      if git tag -l "v$VERSION" | grep -q "v$VERSION"; then
        echo "Tag v$VERSION already exists — skipping (VERSION not bumped)"
        exit 0
      fi
    - git config user.email "ci@code.aws.dev"
    - git config user.name "GitLab CI"
    - git tag -a "v$VERSION" -m "Release v$VERSION"
    - git push origin "v$VERSION"

# Push to GitHub on semver tag (triggered by auto-tag or manual tag)
github-release:
  stage: release
  image: public.ecr.aws/docker/library/python:3.12-bookworm
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
  needs:
    - job: ash
      optional: true
  before_script:
    # $GITHUB_DEPLOY_KEY is base64-encoded (masked + protected CI variable).
    # Base64 strips whitespace/newlines so GitLab accepts masking.
    - apt-get update -qq && apt-get install -y -qq openssh-client
    - eval $(ssh-agent -s)
    # DIAGNOSTIC (remove after v0.1.0 succeeds): confirm GITHUB_DEPLOY_KEY decodes to a plausible key.
    - |
      echo "GITHUB_DEPLOY_KEY raw length: ${#GITHUB_DEPLOY_KEY}"
      DECODED_LEN=$(printf '%s' "$GITHUB_DEPLOY_KEY" | base64 -d 2>/dev/null | wc -c)
      echo "GITHUB_DEPLOY_KEY base64-decoded length: $DECODED_LEN"
      if [ "$DECODED_LEN" -lt 200 ]; then
        echo "ERROR: decoded value is too short ($DECODED_LEN bytes). Either the variable is raw (not base64) or it decodes to garbage. Re-upload the key with: base64 -w0 key | pbcopy"
        exit 1
      fi
    - echo "$GITHUB_DEPLOY_KEY" | base64 -d | ssh-add -
    - mkdir -p ~/.ssh && ssh-keyscan github.com >> ~/.ssh/known_hosts
  script:
    - make -f scripts/util/release.mk release-check
    - git remote add github git@github.com:aws-samples/sample-innovation-patterns.git || true
    - git push github HEAD:main --force-with-lease
    - git push github "$CI_COMMIT_TAG"
